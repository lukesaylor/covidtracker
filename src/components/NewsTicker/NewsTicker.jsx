import React, { useEffect, useState } from "react";
import styles from "./NewsTicker.module.css";

const NewsTicker = ({ stateNews: {title, description, url, urlToImage} }) => {
  console.log(stateNews);

  return (
  {stateNews ? ()}
    )
};

export default NewsTicker;
